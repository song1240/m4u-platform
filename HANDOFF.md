# HANDOFF.md — 세션 인수인계

> 마지막 갱신: 2026-08-20 (인프라 결정 — Supabase 베트남 자체 호스팅)

## 현재 상태

- `src/master-ui/` = **공식 디자인 기준** (Forest·Ivory·Champagne Gold, DM Serif). 라우팅 기본값.
  - 온보딩 3화면(언어 → 소개 → MY ZONE) 이식 완료 (H06-①). **현재 앱에서 실제로 동작하는 유일한 흐름.**
  - 5탭(홈/Living/Habit/Salon/MY)은 **정적 목업** — 아래 점검 결과 참조.
  - 모듈 구조: `i18n.js` / `data.js` / `components.jsx` / `screens/Onboarding.jsx` / `MasterUI.jsx`
- `src/App.jsx` = 프로토타입 v10 (기능 완결, **이전 디자인 — 기능 참조용**, `/#legacy`)
- `src/admin/Admin.jsx` = 본사 Admin Web (`/#admin`)
- `npm run verify` PASS · `npm run build` 성공

## UX 점검 결과 (2026-08-13, 실행 기반 실측)

점검 방법: dev 서버 구동 → 온보딩 자동 통과 → 5탭 순회하며 DOM에서 클릭 핸들러·넘침·터치 타깃·언어 잔존 문자 실측.

**핵심: 5탭의 컨트롤 65개 중 40개가 무반응.** 탭 이동 외에 할 수 있는 동작이 없다.

| 탭 | 죽은 컨트롤 |
|---|---|
| 홈 | 8 (타일 4 · MY DAY · AI 컨시어지 · 혜택 카드 2) |
| Living | 9 (타일 4 · 카트 호출 CTA · 매장 3) |
| Habit | 8 (타일 4 · 챌린지 · 명상 예약 CTA) |
| Salon | 6 (타일 4 · AI PICK · 예약 CTA) |
| MY | 9 (지갑 · 메뉴 5 · 파트너 CTA) |

**P0**
1. 베트남어를 선택해도 5탭이 전부 한국어 (홈에서 한글 79자 검출) — CLAUDE.md §3-6 위반 상태
2. 위 40개 컨트롤 무반응 — H06 ②~⑦ 본작업
3. 온보딩이 "MY 탭에서 언제든 변경할 수 있어요"라고 안내하지만 MY의 `언어`·`MY ZONE 설정` 메뉴가 죽어 있어 **변경 불가** (문구-기능 불일치)

**P1**
4. 새로고침 시 온보딩부터 재시작 (localStorage 미사용 — v10도 동일)
5. 브라우저 뒤로가기가 온보딩 단계와 미연동 → 앱을 벗어남
6. AI 컨시어지 진입점 부재 (홈 AI 카드 무반응 + v10의 FAB 없음) — **미승인, 별도 판단 대기**
7. MY 탭 주 CTA 높이 39px — DESIGN_SYSTEM §7 최소 40px 미달

**P2**
8. 데스크톱(700px↑) 온보딩에서 24px 세로 스크롤 (`.shell` margin-top 24 + `.onboard` 100vh 충돌, 실측)
9. 모바일 주소창 때문에 `100vh` 과대 → `dvh` 적용 검토
10. `<html lang="ko">` 고정 — 베트남어 선택 시에도 ko
11. 홈 인사말 `Good evening` 하드코딩 (시간 무관·이중언어 아님) — **미승인, 별도 판단 대기**
12. 걷기 목표가 마스터 6,000 / v10 10,000 불일치 — **데모 수치이므로 임의 변경 금지**, 기준 확정 필요

**정상 확인**: 온보딩 3화면 흐름·뒤로가기 버튼·존 선택 반영 / 360~820px 가로 넘침 0건 /
한·베 줄바꿈 정상 / `#legacy`·`#admin` 렌더 정상 / verify PASS · build 성공

## 승인된 작업 계획 (2026-08-13, 대표 승인) — 전부 완료

| 순서 | 커밋 | 내용 | 결과 |
|---|---|---|---|
| 1 | `5c23154 [H06]` | 5화면 모듈 분리 + 전면 이중언어 + `<html lang>` 동기화 | ✅ vi 선택 시 한글 0자 (실측) |
| 2 | `637f80e [H06-②]` | 홈 기능 이식 — 타일·카드 실제 탭 이동 | ✅ 홈 무반응 8→1 (AI 카드만 잔여) |
| 3 | `d6874ba [UX]` | MY 언어·ZONE 재설정 + localStorage 복원 + 뒤로가기 연동 + P2 마감 | ✅ 전 항목 실측 확인 |

점검 12건 중 잔여: P0-2(Living·Habit·Salon·MY 나머지 컨트롤 — H06 ③~⑦),
P1-6(AI 컨시어지 진입점 — 미승인), P2-11(Good evening 하드코딩 — 미승인),
P2-12(걷기 목표 6,000 vs 10,000 — 데모 수치, 대표 확정 필요).

## 홈 리디자인 (2026-08-13, 대표 지시)

- 레퍼런스: `~/Downloads/59BC1572-….PNG`(M4U 프리미엄 4폰 목업, 2번째 폰=홈) — 사본 유실 대비, 필요 시 docs/에 보관 권장.
- 홈만 리디자인 (dd6f700). 라우팅·로직·데이터·이동 흐름·하단 5탭·팔레트 무변경. 패턴은 DESIGN_SYSTEM §4.2.
- 6관점 멀티에이전트 비평 패널(타이포/간격/사진/네이티브/브랜드/충실도, 38건) → 전 항목 반영 완료.
- 남은 판단 대기: ① 인사말 시간대 연동(현재 "좋은 저녁이에요" 고정 — P2-11), ② AI 컨시어지 진입점(P1-6,
  셰브런은 제거해 표시 전용임을 명확히 함), ③ 나머지 4개 화면(Living/Habit/Salon/MY)에 §4.2 패턴 확장 여부.
- 사진: Unsplash 임시 8컷 (카트 1548099212 · 다이닝 1414235077428 · 스파 1600334129128 · BBQ 1544025162
  · 카페 1555396273 · 살롱 1562322140 · 스테이 1522708323590). 실서비스 전 실사 교체 필수.

## 고객 여정 전수 검증 (2026-08-13, 실행 기반)

계측 하네스로 실제 클릭 시퀀스를 돌려 6그룹 48개 시나리오 검증 — **48/48 PASS** (그중 2개 무작위 재현으로 교차 확인).

| 그룹 | 범위 | 결과 |
|---|---|---|
| A 온보딩(ko) | 3단계 상태기계·둘러보기·화면/브라우저 뒤로·존 3종·저장/복원·fresh 초기화 | 9/9 |
| B 온보딩(vi)+전 탭 | vi 여정·5탭 한글 0자·MY 육안(줄바꿈·성조 정상) | 7/7 |
| C 홈 컨트롤 전수 | 12개 목적지 전부 일치·가로 레일 실스크롤·dead 0·이미지 7/7 로드 | 13/13 |
| D MY 재설정 | 언어 변경·존 변경/취소(뒤로 2경로)·미연결 메뉴 무반응·탭 왕복 | 8/8 |
| E Living/Habit/Salon | dead 5/5/5(=H06 ③~⑤ 대기 기대값, 회귀 없음)·육안·살롱 히어로 렌더 | 5/5 |
| F 반응형·라우트 | 820px 스크롤 없음·#legacy·#admin·/ 스모크·홈 이미지 전수 | 6/6 |
| (인라인) 360px | ko 홈·vi MY 가로 넘침 0·이미지 정상 | PASS |

미연결 컨트롤 현황(모두 하네스 대기 상태, 결함 아님): Living 5 · Habit 5 · Salon 5 · MY 4 (H06 ③~⑦).

## H06-③ Living 이식 완료 (2026-08-13)

- 화면: `screens/Living.jsx`(6타일 + 랭킹 목록) · `Category.jsx`(카테고리/MOVE/STAY) · `Venue.jsx`(상세 3탭) · `Book.jsx`(예약→완료)
- 데이터: `data.js`(CATS·LIVING_TILES·VENUES 5건·서비스·리뷰) · `ranking.js`(Consumer First 산식 분리)
- 배선: 서브화면 히스토리 라우팅(뒤로가기로 닫힘, 탭 전환 시 자동 종료) · 토스트 · 예약 적립이 HRP 잔액에 즉시 반영(실측 125,800 → 125,825)
- **정책 교정**: v10은 신규 파트너 boost를 3번째에 삽입했으나 POLICY §1·H02는 "2번째" — 이식본은 정책대로 2번째(`ranking.js`의 `BOOST_POS=1`). v10 레거시는 참조용이라 미수정.
- 미연결(의도): Living 목록의 zone 필터(v10도 전 존 공통), SHOP 상품 상세·STAY 객실 예약(H06 ⑥⑦ 및 별도 하네스), 공유 버튼(죽은 컨트롤 방지로 제거)

## H06-④ Habit 이식 완료 (2026-08-13)

- 5습관 실동작: 걷기(검증형 — 동기화 → 목표 달성 시 +10 HRP·+2 CP) + 셀프 4종(물 8잔·달리기·마스크팩·명상, 각 +5 HRP)
- **정책 교정 2건째**: POLICY §5의 셀프 일일 상한 15 HRP를 v10은 캡션 문구로만 안내하고 실제로는 20 HRP를 지급했다.
  이식본은 `SELF_DAILY_CAP=15`로 **강제** — 실측: 어떤 순서로도 셀프 합계 15 (125,815 / 걷기 포함 125,825).
  초과분은 지급하지 않고 토스트로 사유를 안내한다. 하루 1회·해제 불가도 코드로 보장.
- 주간 리포트(7일 막대, 오늘은 실제 완료 수) · 주변 명상 장소 3곳 → 예약 시 지역 소비·Verified Review로 연결(POLICY §5)
- 걸음 수는 홈과 공유 — Habit에서 동기화하면 홈 MY DAY 카드에 즉시 반영
- 검증형에만 Verified 뱃지·CP 표기, 셀프 체크에는 CP 미표기 (§4.4 규칙)

## H06-⑤ Salon 이식 완료 (2026-08-13)

- 4서비스 퀵(헤어·네일·스킨케어·마사지) → 각 서비스 예약으로 직행, 추천 살롱 → 매장 상세
- 뷰티 프로필: 3문항 설문 → **응답에 따라 결과가 실제로 분기**(건성/복합성/지성, 지표 3종 계산) → 케어 예약·매장 보기 추천
- POLICY §5 준수: "피부 분석 / 뷰티 프로필"만 사용, 설문·결과 화면 최상단에 "의료 진단이 아니라..." 상시 노출(실측 확인)
- verify 금지어 스캔이 **주석에 쓴 금지 표현까지 차단**해 FAIL — 주석 문구를 수정해 통과. 스캐너가 문맥을 가리지 않는 것이 의도된 동작.

## H06-⑥ 지갑 이식 완료 (2026-08-13)

- 진입: MY의 지갑 카드 → 지갑 서브 화면 (5탭 고정이라 별도 탭을 만들지 않음)
- 자산 카드: HRP·CP 좌우 분리, CP는 "구매·양도 불가 + 투표 가중치"를 카드에 상시 명시 (POLICY §4)
- 적립 내역: HRP/CP 세그먼트 탭 + CP 탭에 적립 규칙 표 — 실측으로 예약 적립이 내역에 남는 것 확인(HRP 125,825 / CP 131)
- FIVE 3방: 정책 수치를 그대로 사용(5/10/15인 · 40/45/50% · 20/40/60 HRP · 72/96/120시간),
  "구매 건수 비례 보상 · 미달 시 자동 취소·전액 환불"을 목록과 참여 시트 양쪽에 노출 (POLICY §6)
- 투표: CP 가중 집계, 투표해도 CP가 차감되지 않음을 명시. 정족수 진행바·가결 상태 표시
- **verify 강화**: CLAUDE.md §3-2의 코인/토큰 금지가 스캐너에 없었다 → 규칙 추가(Admin은 내부 정책 서술이라 예외).
  추가 직후 제 주석을 FAIL로 잡아 수정 — 규칙이 실제로 작동함을 확인.
- **버그 수정**: 서브 화면이 2단 이상(매장→예약) 쌓였을 때 탭 전환이 한 단계만 닫혀 상세가 남던 문제 →
  히스토리 상태에 깊이를 담아 `history.go(-depth)`로 앱 루트까지 한 번에 복귀.

## H06-⑦ MY + 파트너 완료 (2026-08-13) — **H06 전 항목 종료**

- MY 재작성(프로덕션 패턴): 예약 내역·쿠폰(지갑)·투표·MY ZONE·언어 전부 실제 연결. 미구현은 "준비 중"으로 명시(죽은 컨트롤 0)
- 파트너 등록 4단계: 역할 4종 → 자유 서술 → **AI 구조화 초안**(업종·가격·운영시간·예약형태) → 필수 확인 3종 → 제출 → 승인 시 **+50 CP**
  - v10의 운영시간 파싱이 "아침 7시~저녁 8시"를 07:00-08:00으로 읽던 것을 오전/오후 보정으로 수정(07:00-20:00)
- MY BUSINESS: 역할별 운영 화면 + **CONTRIBUTOR CENTER**(H07 흡수)
  - HOST: 카테고리 **집계만** 노출, 개별 결제 비노출·1단계 한정 문구 상시 표시 (POLICY §7)
  - Contributor: 퍼널(스캔→가입→첫이용→Verified Review) · Score · QR 타입 · **랭킹 방화벽·90일 시효·자기 유입 제외** 문구 (POLICY §10)

## H06 이후 남은 것 (대표 판단 대기)

| 항목 | 상태 |
|---|---|
| ~~AI 컨시어지 진입점~~ | ✅ 완료 — 안내+초안 범위(대표 승인). 결제·확정·리뷰·투표는 AI 불가 |
| ~~상품 상세·장바구니·주문(SHOP)~~ | ✅ 완료 — M4U Select 4상품·장바구니·결제·주문 내역, 결제 시 HRP 적립 |
| ~~STAY 객실 예약~~ | ✅ 완료 — 3객실·기간 선택·결제액 비례 HRP·+5 CP, QR 체크인은 레지던스 화면 |
| ~~Verified Review 작성 화면~~ | ✅ 완료 — 예약 내역에서만 진입(POLICY §2 구조 강제), 별점 무관 +5 CP |
| ~~홈 인사말 시간대 연동~~ | ✅ 완료 — 생활권 시간(ICT) 기준 4구간 |
| 사진 전량 Unsplash 임시 | 실서비스 전 실사 촬영 필요 |

## H02~H04 회귀 확인 (2026-08-13)

17개 수용 기준을 코드 검사 + 실행으로 대조 → **11 충족 / 6 미충족**, 그중 5건 즉시 수정 완료.

**수정 완료**
- 홈 HRP 배지 · 혜택 카드 → 지갑 직행 (H03 "홈 포인트 배지·MY 카드로 진입")
- "CP는 구매 불가 · 추후 온체인 거버넌스 이관 전제" 문구 복원 (H03) — 코인/토큰 표현은 쓰지 않음
- 카운트다운 실동작 `<Countdown>` 신설 (1분 간격 갱신, 24h 미만 경고색) — FIVE 모집·투표 마감 (H03)
- HOST Recommended Stay 뱃지 + "이용률·만족도 기반 · 구매 불가" (H04 · POLICY §7)

**전부 완료 (2026-08-13)**
- H04 MY BUSINESS 카트 운영: ONLINE 토글 · 호출 수락 시 매출/건수 실반영(320,000→340,000 실측) · AI Manager(가격 25,000 변경·운영 중단 실동작)
- H04 투숙객 측: 레지던스 화면 — QR 체크인 · 컨시어지 4종 · ROOM Guest QR 연결 카드 + 귀속/프라이버시 문구
- → **H02~H04 수용 기준 17개 전부 충족**
- (참고) 홈 명상 클래스 카드 · 프로필→제품 추천은 상품/스테이 화면 미이식에 종속 — 아래 표 참조

**작업 메모**: 이번에 문자열 치환이 2회 조용히 실패(H06-④에서 `steps`→`totalSteps`로 바뀐 것을 몰랐음).
이후 모든 치환은 `assert count==1`로 적용 여부를 강제 확인한다.

## Verified Review 작성 신설 (2026-08-13)

**소비자 루프의 마지막 고리** — CLAUDE.md §1의 `이용/결제 → Verified Review → HRP → 재이용`이 이제 앱에서 실제로 돈다.
v10에도 없던 화면이라 이식이 아니라 신규 구현이며, POLICY §2를 **구조로** 강제했다.

- 진입은 예약 내역에서만 — 미이용 계정은 작성 경로 자체가 없다
- 별점은 보상과 무관(1점도 +5 CP 동일 — 실측), "별점은 보상과 무관합니다"를 항상 노출
- 최소 20자 미달 시 제출 불가, 반복·대량 작성은 검토 대상임을 고지
- 작성한 리뷰는 매장 상세 리뷰 탭에 즉시 반영(리뷰 수 128 → 129 실측), 예약 내역엔 "리뷰 완료" 뱃지
- 실측: CP 128 → 136 (예약 +3, 리뷰 +5)

## SHOP 이식 완료 (2026-08-13)

- 진입: Living의 SHOP 타일 → SHOP 화면(장바구니 · 주문 내역 · M4U Select 4상품 · MY ZONE 스토어)
  - SHOP 타일이 카테고리 목록 대신 상품 화면으로 가도록 라우팅을 바꿨고, 그린마켓 접근 경로는
    "MY ZONE 스토어" 섹션으로 유지했다(도달 불가 분류가 생기지 않도록).
- 상품 상세: 히어로 · 공식 뱃지 · 가격/원가/할인율 · 태그 3종(HRP·무료배송·반품) · 아코디언(구성/설명) · 담기|바로구매
- 장바구니: 수량 스테퍼(±40px) · 명세(상품금액·배송비·적립 예정·총액) · 결제
- 실측: 수량 2개 → 1,240,000 VND·+124 HRP(비례 정확), 결제 후 HRP 125,800 → 125,862,
  주문 내역 1건 생성·장바구니 비움, 바로구매 +19 HRP, 빈 장바구니 안내, 한/베 렌더
- `.ctabar > .two` 셀렉터 오작성으로 2열 CTA가 어긋났던 것을 스크린샷에서 발견해 수정

## STAY 객실 예약 이식 완료 (2026-08-13)

- 진입: Living STAY 타일 → STAY 화면(내 스테이 · 레지던스 객실 3종). 기존 "준비 중" 안내 화면을 대체
- 예약: 객실 상세(편의 4종·설명) → 체크인 날짜 칩 + 숙박일 스테퍼 → 명세 → 확인 시트 → 완료
- 적립: 결제액 비례 HRP + **스테이 완료 +5 CP** (POLICY §4). 완료 화면에 체크인 당일 QR 발급 안내와
  "투숙 기간 이용은 Host에게 1단계 추천 보상 귀속 · 본인 결제 제외" 문구 (POLICY §7)
- 실측: 3박 4,350,000 VND·+435 HRP → 4박 5,800,000·+580 HRP(비례 정확), 2BR 6,900,000·+690 HRP,
  확정 후 HRP 125,800 → 126,235, 내 스테이 1건, vi 로케일 표기(4.350.000), 전 탭·MOVE·EAT 회귀 없음
- "Sauna"를 번역하지 않아 i18n 경고가 1건 늘었던 것을 실제 베트남어(Phòng xông hơi)로 교정

## AI 컨시어지 진입점 (2026-08-13, 대표 승인)

**승인 범위: 안내 + 초안까지.** AI는 대행하지 않는다.

| AI가 하는 것 | AI가 하지 않는 것 |
|---|---|
| 의도 파악 · 후보 제시(**랭킹 그대로**) · 화면 딥링크 | 결제 · 예약/주문 확정 · 쿠폰 사용 |
| 통역 안내 · 파트너 운영 변경(기구현) | 리뷰 작성 · 투표 |

- FAB는 골드 원형(nav 위 우하단). **하단 고정 CTA가 있는 화면에서는 숨긴다** —
  매장 상세 · 예약 · 상품 상세 · 장바구니 · 객실 예약 · 리뷰 작성 · 파트너 등록 (실측 확인)
- 홈의 AI 카드도 같은 시트를 연다. 의도 매칭 8종(맛집·살롱·이동·객실·상품·지갑·습관·통역)
- 시트 하단에 "AI는 안내와 초안까지만 · 추천 순서는 Consumer First Ranking 그대로"를 **상시 노출**
- 후보 제시는 `ranking.js` 결과를 그대로 사용 — AI가 별도 정렬하지 않는다 (POLICY §1 방화벽 유지)
- 데모 규칙 기반이며 실서비스는 n8n + LLM으로 대체 예정. 규칙은 `data.js`의 `AI_INTENTS`에 분리

## 홈 인사말 시간대 연동 (2026-08-13)

**기기 시간이 아니라 생활권 시간(Asia/Ho_Chi_Minh) 기준**으로 인사한다.
M4U 사용자는 빈홈 그랜드 파크에 살고 ZONES 3곳이 모두 호치민이므로, 한국에서 시연해도
현지 시간으로 보이는 것이 제품에 맞다. `i18n.js`의 `zoneHour()`가 `Intl` 시간대 변환을 담당한다.

| 구간 | 시각 | 한국어 | 베트남어 |
|---|---|---|---|
| morning | 05–10 | 좋은 아침이에요 | Chào buổi sáng |
| afternoon | 11–17 | 좋은 오후예요 | Chào buổi chiều |
| evening | 18–22 | 좋은 저녁이에요 | Chào buổi tối |
| night | 23–04 | 편안한 밤 되세요 | Chúc ngủ ngon |

실증: `TZ=Asia/Seoul`에서 기기 12시(오후)일 때 인사말은 현지 10시 기준 "좋은 아침이에요"로 표시됨.
24시간 전 구간 단위 검증 완료.

## 습관 화면 검증 표시 수정 (2026-08-13, 대표 지적)

**지적**: "누르기만 하면 인증되고, 모두" — 확인 결과 사실이었다.

| 문제 | 조치 |
|---|---|
| `동기화` 2번이면 "헬스 데이터 검증" 뱃지와 함께 **+2 CP 지급** — 연동이 없는데 검증이라 표시 | 건강 앱 **연동 상태**를 도입. 미연동이면 동기화 불가 · CP 미지급 · 뱃지 "미연동 · CP 없음" |
| 셀프 4종이 검증형과 시각적으로 구분되지 않음 | 행마다 "자기 신고 · CP 없음" 회색 뱃지 |
| (수정 중 발견) 미연동 문구가 도달 불가한 상태를 안내 | "건강 앱을 연결하면 걸음이 기록되고 보상을 받아요"로 교정 |
| (수정 중 발견) 비활성 버튼이 활성처럼 보임 | `:disabled` 투명도 규칙을 DESIGN_SYSTEM §7에 추가 |

POLICY §4의 "CP는 검증 가능한 활동에만 규칙 기반 자동 적립"이 이제 코드로 강제된다.
③ 명상 건은 **접근을 바꿔 해결**했다 (아래 참조).

## 명상 퍼널 연결 (2026-08-13)

당초 "명상 셀프 체크 제거"를 제안했으나, 확인해 보니 **더 근본적인 문제**가 있었다 —
"주변 명상 장소"의 예약 버튼이 토스트만 띄우고 실제로는 아무것도 하지 않았다(예약·적립·매장 없음).
그래서 셀프 체크를 없애는 대신 **명상 장소를 진짜 매장으로 만들어 퍼널을 실제로 돌게** 했다.

- MEDI_PLACES 3곳(Mindful Garden · Lotus Yoga · Silence Retreat)을 `VENUES`에 `cat:"life"`로 승격.
  각각 클래스 서비스 1종 + 편의 4종 + 정책 문구를 갖춘 정식 매장이다.
- Habit의 "클래스 예약" → 매장 상세 → 기존 Book 플로우(+HRP·+3 CP) → 예약 내역 → Verified Review.
  POLICY §5의 `명상 → 예약 → 지역 소비 → Verified Review` 퍼널이 이제 실제로 돈다.
- 클래스를 예약하면 명상 습관이 완료되고 뱃지가 "자기 신고" → **"클래스 예약"**(검증형)으로 바뀐다.
  셀프 체크와 중복 집계하지 않는다.
- **덤**: `cat:"life"` 매장이 0건이라 빈 화면이던 Living EXPERIENCE 카테고리가 3곳으로 채워졌다.

습관은 5종 그대로 유지된다 — 매일 하는 셀프 명상은 남기되 클래스 예약이 더 나은 경로임을 보여준다.

## FEED 커뮤니티 1단계 (2026-08-13, 대표 지시)

**지적**: "자기 신고 방식이 이상하다 · 피드가 없다 · 커뮤니티가 없다"

POLICY에 커뮤니티 조항이 **한 줄도 없어서**, 개정 없이 가능한 1단계만 먼저 구현했다.

- **FEED 탭 신설** (5탭 → 6탭, DESIGN_SYSTEM `nav` 개정). 순서: 홈 · Living · Habit · Salon · FEED · MY
- **셀프 습관 3종(달리기·마스크팩·명상)은 한 줄 기록을 남겨야 완료된다** — 탭만으로는 완료되지 않는다.
  물마시기만 +1잔 탭을 유지(8잔을 매번 기록하는 건 비현실적).
- 보상은 해당 습관의 HRP 그대로(수치 불변) · **CP 없음** · 셀프 일일 상한 15 HRP 공유
- 기록은 사진 첨부 선택 가능(`input[type=file]` + objectURL, 신규 의존성 없음)

### 정책 방화벽 (구현에 반영)
- 피드에는 **별점이 없다.** 매장을 언급해도 평점 · 랭킹 · 검색에 **반영하지 않는다** (§1·§2·§10)
- 화면에 "기록과 리뷰는 다릅니다 — 매장 평가는 Verified Review로만 반영됩니다"를 상시 노출
- 게시물에 CP를 주지 않는다 — 사진은 검증 가능한 활동이 아니다 (§4)

## POLICY §11 신설 · §6 개정 (2026-08-13, 대표 승인 완료)

3차 검토를 거쳐 승인·반영되었다. 이제 FEED 2단계는 **정책 공백이 아니다.**

### 확정된 4개 역할 분리

| | 역할 |
|---|---|
| **FEED** | 발견 · 공유 (평가 아님) |
| **Verified Review** | 공식 평가 — 실제 예약·결제자만 (§2) |
| **HRP** | 실제 행동 보상 (§3·§5) |
| **Consumer First Ranking** | 사업자 품질 평가 (§1) |

### 대표 지시로 초안에서 바뀐 것 (기록 — 같은 실수 반복 금지)

1. **`Verified 이용` → `M4U 이용 확인`** — `Verified` 단어를 쓰면 Verified Review와 혼동된다.
2. **게시 자체 무보상** (가장 중요) — 초안은 게시에 HRP를 줬다. 그러면 FEED가 맛집·생활정보로
   넓어질 때 **보상 채굴 구조**가 되어 피드 품질이 무너진다.
   → `글을 써서 HRP를 받는다`(X) / **`실제 행동을 하고 그 기록이 FEED에도 남는다`(O)**
3. **`~로만 Ranking이 올라간다` 금지** — §1의 취소율·이행률·응답품질 등을 나중에 쓸 때 충돌한다.
   §11.7은 §1을 **참조만** 한다.
4. **방화벽 범위를 좁힘** — "검색 품질" → "**사업자·매장·서비스의 공식 검색/추천 순위**".
   FEED 내부 정렬(최신·관련·인기글), FIVE 내부 상품 정렬은 막지 않는다.
5. **Challenge 삭제** — POLICY에 정의가 없는데 HRP 지급 사유로 넣으면 §4에 구멍이 난다.
   실제 도입 시 **§5.x에 `기간·인증방식·HRP·상한·실패조건`을 정의한 뒤** §11.6에서 참조할 것.
6. **`데모 자리표시자` 표현 금지** (POLICY 문서 한정) → "파일럿 운영 기준값이며 대표 승인 후 조정 가능".
7. **개인정보는 POLICY에서 분리** — IP/기기 수집 방식은 별도 **Privacy/Abuse Spec**으로 뺀다.
8. **법률 단정 금지** — "방문판매법 리스크 회피"(단정) → "관련 법적 리스크를 낮추기 위한 **내부 원칙**이며
   **실제 운영 전 법률 검토 필요**". 방문판매법 판단은 1단계 여부만으로 끝나지 않는다.
   (`docs/POLICY.md:40` · `data.js` FIVE 주석 2곳 수정 완료)

### FEED 2단계 구현 상태

| 항목 | 근거 조항 | 상태 |
|---|---|---|
| 맛집·생활정보 게시(습관 외) | §11.2 · §11.6 | **완료** — 카테고리 5종 · **보상 없음** |
| `M4U 이용 확인` 표시 | §11.4 | **완료** — 예약 기록에서만 산출 · 자가 부착 불가 |
| `PARTNER` 표시 · 파트너 게시 | §11.7 | **완료** — 뱃지 강제 |
| 좋아요 · 댓글 | §11.5 · §11.6 | **완료** — 보상 없음 · `RANKING_FIELDS`로 순위 유입 차단 |
| 신고 · 차단 · 삭제 | §11.8 | **완료** |
| Privacy/Abuse Spec | §11.9 | 초안 + **자문 요청서 발송 대기** — `docs/LEGAL_REVIEW_REQUEST.md` |
| 팔로우 | §11.5 | 미구현 — 필요성부터 판단 |
| **브랜드 FIVE 개설** | §6.1~6.4 | **완료** — Admin 검수 화면 + 앱 개설 주체 표시 |

### 다음 세션이 알아야 할 것

- **Challenge는 정의가 없다.** §11.6에서 의도적으로 뺐다. 도입 시 **§5.x에
  `기간·인증방식·HRP·상한·실패조건`을 먼저 정의**한 뒤 연결한다.
- **PRIVACY_ABUSE_SPEC은 초안이다.** 디바이스 식별자·IP는 "수집하지 않음"이 기본값이며,
  필요성 입증과 법률 검토를 통과한 뒤에만 추가한다. "일단 모아두고 나중에 판단"은 하지 않는다.
- **Supabase 리전을 확정하기 전에 자문 Q2(국외 이전) 답을 받아야 한다.** 베트남 개인정보 보호법이
  2026-01-01자로 Law 91/2025/QH15 로 바뀌었고(Decree 13/2023 폐지), 국외 이전 영향평가(CTIA)
  제출 의무와 제재 수준이 스택 결정에 직접 영향을 준다. `docs/LEGAL_REVIEW_REQUEST.md` 참조.
- **걸음 수를 서버에 저장할지 결정 전이다.** 민감정보로 분류되면 "목표 달성 여부(true/false)"만
  저장하는 설계로 부담을 줄일 수 있는지 자문 Q3에 물어뒀다.
- 랭킹 방화벽은 이제 `ranking.js`의 `RANKING_FIELDS` 화이트리스트로 고정돼 있다.
  점수 계산에 필드를 추가하려면 **POLICY §1 개정이 먼저다.**

## 프로덕션 품질 3종 (2026-08-20, 대표 승인) — **3/3 완료**

"Home은 리디자인했지만 전체 앱 일관성이 더 필요하다"는 지적에서 출발. **실측 결과 지적의 진단은
이 저장소에 맞지 않았다** — style.css 1개(80클래스)를 26개 화면이 공유하고, 인라인 스타일 6개는
전부 동적 값(진행바 너비·차트 높이)이며, 탭 루트 6개가 같은 appbar/greet 구조를 쓴다.
"홈만 새 디자인"인 것은 `/#legacy`의 v10 얘기다.

실제 프로덕션 격차는 다음이었고, 순서대로 처리한다.

| | 항목 | 상태 |
|---|---|---|
| 1 | **모션** — transition이 3개(아코디언·토글)뿐, 화면 전환 0 | **완료** (§9) |
| 2 | **이미지 상태** — onError 0, 스켈레톤 0. Supabase 연결 시 전부 드러남 | **완료** (`<Photo>`) |
| 3 | **접근성** — 아이콘 전용 버튼에 이름이 없음 | **완료** (§7.1) |

> **정정**: 3번을 처음에 "alt=\"\" 24개"라고 적었으나 **틀렸다.** 마크업을 확인하니 모든 사진에
> 같은 정보를 담은 텍스트가 옆에 붙어 있어 **`alt=\"\"`가 WCAG상 맞다.** 이름을 넣으면 스크린리더가
> 같은 말을 두 번 읽는다. 3번의 실제 대상은 **아이콘만 있는 버튼**(뒤로·닫기·좋아요·수량±)이다.
| — | **실사 촬영** — 사진 22장 전부 Unsplash 스톡(도메인 1개) | **코드 밖 · 최대 차단 요인** |

사진 문제가 1순위인 이유: M4U는 Verified Review로 "진짜"를 파는 앱인데 매장 사진이 남의 스톡이면
첫 화면에서 전제가 무너진다. 레이아웃을 아무리 다듬어도 이게 남으면 프로덕션으로 보이지 않는다.

**하지 않기로 한 것**: 홈 스타일을 다른 화면에 복사하는 전면 리디자인. 이미 같은 시스템을 쓰고 있어
얻을 게 없고 CLAUDE.md §6이 금지한 '하네스 없는 대규모 리팩터링'에 해당한다.

## 인프라 결정 (2026-08-20, 대표 결정)

**개인정보 원본은 베트남 국내에 둔다.**

그런데 **Supabase 호스팅에는 베트남 리전이 없다.** 전 리전이 AWS 기반이고 아시아·태평양은
뭄바이 · 싱가포르 · 도쿄 · 서울 · 시드니뿐이다(AWS에 베트남 리전이 없어서 생기는 제약).
따라서 **베트남 클라우드에 Supabase를 자체 호스팅**한다.

| 검토안 | 결과 |
|---|---|
| **A. 베트남 클라우드에 Supabase 자체 호스팅** | **채택** — 스택과 CLAUDE.md §2 유지, 데이터 국내 보관 |
| B. 베트남 관리형 Postgres + 백엔드 직접 구축 | 기각 — Auth·Storage·Realtime 재구현 부담이 큼 |
| C. 싱가포르 리전 + 국외 이전 신고 | 기각 — 국내 보관 방침과 반대 |

**대가**: 업그레이드 · 백업 · 이중화를 직접 운영해야 한다. 파일럿(단지 하나) 규모에서는
VM 한 대 + 관리형 백업으로 감당 가능하다고 판단했다.

**미정**: 클라우드 사업자 (Viettel IDC · FPT Cloud · VNPT · CMC 등 비교 필요)

### 국내 보관으로도 안 풀리는 것 — 자문 Q2

데이터가 베트남에 있어도 아래는 남는다. `docs/LEGAL_REVIEW_REQUEST.md` Q2-1~Q2-7 참조.

- **한국 개발·운영팀의 원격 접속**이 국외 이전에 해당하는가 (물리적 이동 없이 접근만으로도?)
- 백업본 · 로그를 국외에 두면 이전인가
- 국외 SaaS — 푸시 알림(FCM/APNs) · 오류 추적(Sentry) · 지도 · 결제 게이트웨이
- 자체 호스팅 시 요구되는 기술적 · 관리적 안전조치 기준

## BLOCKED

(없음)

## 메모

- 모든 수치(HRP·CP·수수료 8%·Host 보상 2%·90일 시효 등)는 데모 자리표시자. 단위 경제 검증 전 변경 금지.
- 이미지가 Unsplash 임시 소스 — 실서비스 전 매장별 실사 촬영 필요.
- **verify의 한계**: 빌드 검증이 lucide-react를 external로 두어 없는 아이콘명을 못 잡는다
  (`House`가 0.383에 없어 빌드 실패 상태였고 `Building2`로 수정). 아이콘 추가 시 `npm run build` 병행 필수.
- 온보딩 선택값은 `localStorage("m4u.master.v1")`에 저장 — 새로고침해도 앱에서 시작. `?fresh=1`로 초기화 가능.
- **작업 방식(대표 지시)**: 모든 작업은 시작 전 점검 결과·수정 포인트를 보고하고 승인받은 범위만 진행한다.
